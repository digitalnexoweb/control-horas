{
  "name": "control-horas",
  "private": true,
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "build": "echo 'Build ready'",
    "deploy": "wrangler pages deploy frontend --project-name=control-horas",
    "preview": "wrangler pages dev frontend"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.95.3"
  },
  "devDependencies": {
    "pm2": "^6.0.14",
    "wrangler": "^4.95.0"
  }
}
